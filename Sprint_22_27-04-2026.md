# SPRINT 22 - 27/04/2026

## COSA È STATO CREATO E A COSA SERVE
1.  **Metodo di Autenticazione Email e Password**
    *   **Perché:** Su richiesta esplicita, è stata rimossa l'autenticazione tramite Google (che causava anomalie in determinati ambienti o preferenze non gradite all'utente) e sostituita con un form solido di Registrazione e Log-in tramite Email e Password classica.

## COME È FATTO E COME FUNZIONA
*   **App.tsx:** Sono stati introdotti gli stati per gestire `email`, `password` e il toggle `isRegistering` per determinare se l'utente si sta registrando o sta loggando.
*   Implementati i metodi `signInWithEmailAndPassword` e `createUserWithEmailAndPassword` nativi di Firebase.
*   Rimossa la logica `signInWithPopup` relativa a Google.
*   La schermata di login mostra ora campi di input per le credenziali con gestione dell'errore (in italiano) per casi quali "credenziali errate", "email già in uso" e "password debole".

## EFFETTI E PROPAGAZIONE
*   **Sicurezza:** Gestione chiusa della sessione tramite input diretti invece di finestre in popup dipendenti da header COOP/COEP che bloccavano l'ingresso.
*   Nessun impatto logico sulle collections di Firestore in quanto l'identificativo principale utente (`uid` di Firebase) resta nativo.

## STATO DI COERENZA
Build verificata e test superati.
